# Host venv dependencies (mirrors requirements-host.txt).
# Devs who run `pip install -r requirements.txt` get the host venv only.
#
# Per-engine requirements are in separate files:
#   requirements-chatterbox.txt  — Chatterbox Turbo + Full engines
#   requirements-higgs.txt       — Higgs Audio engine (transformers<4.47.0)
#   requirements-qwen3.txt       — Qwen3-TTS engine (transformers>=4.57.3)
#
# To set up all venvs at once, run: bash scripts/setup_venvs.sh

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.0
soundfile>=0.12
numpy>=1.24
scipy>=1.10
